/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com',"static.vecteezy.com","firebasestorage.googleapis.com"], 
      },
};

export default nextConfig;
